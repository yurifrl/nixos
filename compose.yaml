x-base-service: &base_service
  build:
    context: .
    args:
      DROPLET_IP: $DROPLET_IP
  network_mode: host
  environment:
    DROPLET_IP: $DROPLET_IP
    SSH_AUTH_SOCK: /ssh-agent
    NIXPKGS_ALLOW_BROKEN: 1
    NIXPKGS_ALLOW_UNSUPPORTED_SYSTEM: 1
  volumes:
    - .:/workdir
    - $SSH_AUTH_SOCK:/ssh-agent
    - nix-cache:/nix-cache

volumes:
  nix-cache: {}

services:
  sh:
    <<: *base_service
    entrypoint: bash
  gatus:
    image: twinproduction/gatus
    ports:
      - "8080:8080"
    volumes:
      - ./modules/gatus/config.yaml:/etc/gatus/config.yaml
