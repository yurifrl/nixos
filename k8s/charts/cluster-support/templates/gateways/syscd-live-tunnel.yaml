# Tailscale domain
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: syscd-live-tunnel
  namespace: istio-system
spec:
  selector:
    istio: gateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "*.syscd.live"
      tls:
        httpsRedirect: false
    - port:
        number: 443
        name: https
        protocol: HTTPS
      hosts:
        - "*.syscd.live"
      tls:
        mode: SIMPLE
        credentialName: argocd-server-tls