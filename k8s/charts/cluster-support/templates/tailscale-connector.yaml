apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: tailscale-connector
  namespace: tailscale
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  hostname: syscd-tech-01
  tags:
    - tag:k8s
  subnetRouter:
    advertiseRoutes:
      - "10.42.0.0/16"
      - "10.43.0.0/16"
