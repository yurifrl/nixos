{{- range .Values.dashboards }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .name | trunc 63 }}
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
  annotations:
    file: {{ .file }}
data:
  {{ .file }}: |
    {{- $.Files.Get (.file | quote) | nindent 4 }}
{{- end }}