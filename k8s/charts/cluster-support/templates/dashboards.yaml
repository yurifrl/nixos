{{- range .Values.dashboards }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .name | trunc 63 }}
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
    grafana_folder: "syscd"
data:
  dashboard.json: |
    {{- $.Files.Get .file | nindent 4 }}
{{- end }}