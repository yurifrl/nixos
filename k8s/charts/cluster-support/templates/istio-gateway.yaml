apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: public
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
  - hosts:
    - "*.homey.cloud"
    - "homey.cloud"
    port:
      name: http
      number: 80
      protocol: HTTP 