apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mqttui.serviceAccountName" . }}
  labels:
    {{- include "mqttui.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}