apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudflare-tunnel
  namespace: argocd
spec:
  project: default
  source:
    chart: cloudflare-tunnel
    repoURL: https://cloudflare.github.io/helm-charts
    targetRevision: 0.3.2
    values:
      cloudflare:
        account: foo
        tunnelId: bar
        secretName: tunnel-secret
  destination:
    server: https://kubernetes.default.svc
    namespace: cloudflare-tunnel
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true 

# cloudflared tunnel login
# cloudflared tunnel create nixos-1
# kubectl -n cloudflare-tunnel create secret generic tunnel-secret --from-file=credentials.json=/home/nixos/.cloudflared/...  