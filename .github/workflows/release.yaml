name: Release

on: [push]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Builder Docker Image
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Add Tailscale token to /etc/secrets
        run: | 
          mkdir secrets
          echo ${{ secrets.TAILSCALE_TOKEN }} | sudo tee ./secrets/tailscale-token

      # QEMU is inside this image
      - name: Build NixOS SD Image
        run: |
          docker compose run --rm hs nix build

      - name: Move Image to Artifact Directory
        run: |
          mkdir -p artifacts
          cp result/*.img* ./artifacts/

      # Push Both Artifacts
      ## Push docker image
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHA_PAT }}
    
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/yurifrl/home-systems:latest

      ## Nixos SD Image
      - name: Upload Image as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: nixos-sd-image
          path: ./artifacts/*.img*
